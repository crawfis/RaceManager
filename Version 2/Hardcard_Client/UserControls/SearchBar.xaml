<UserControl x:Class="RacingEventsTrackSystem.UserControls.SearchBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    
<Border Background="{StaticResource lightBlueBrush}"
        CornerRadius="6"
        Margin="0"
        Padding="4">
        <DockPanel LastChildFill="False">
            
            <TextBlock DockPanel.Dock="Left"  Text="Event: "  Foreground="White"  VerticalAlignment="Center"  FontSize="18"  FontFamily="Trebuchet" FontWeight="Bold" />
            <TextBlock DockPanel.Dock="Left" Text="{Binding CurrentEvent.EventName}" Width="300" Name="txblEventName"     FontWeight="Bold"   Foreground="White"  VerticalAlignment="Center"  FontSize="18"  FontFamily="Trebuchet" />
            
            <!--<TextBox x:Name="searchText" DockPanel.Dock="Right" Width="150" Background="White"  TextChanged="SearchText_Changed" />
            <Label DockPanel.Dock="Right" Content="Search:"  FontWeight="Bold"   Foreground="White" />-->
        </DockPanel>
    </Border>
</UserControl>
